# Multicloud Object Gateway (NooBaa) S3 API is exposed in-cluster as Service s3.openshift-storage.
# Bucket name must match ObjectBucketClaim spec.bucketName (see 02-noobaa-obc.yaml).
apiVersion: oadp.openshift.io/v1alpha1
kind: DataProtectionApplication
metadata:
  name: dr-poc-dpa
  namespace: openshift-adp
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-wave: "2"
spec:
  backupLocations:
    - velero:
        provider: aws
        default: true
        credential:
          name: cloud-credentials
          key: cloud
        objectStorage:
          bucket: velero-gitops-dr-poc
          # OADP requires a prefix starting with "velero" unless configuration.backupImages is false.
          prefix: velero
        config:
          region: us-east-1
          s3Url: https://s3.openshift-storage.svc.cluster.local
          s3ForcePathStyle: "true"
          # Velero pods do not trust the in-cluster S3 service CA by default; required for NooBaa S3 on many clusters.
          insecureSkipTLSVerify: "true"
  configuration:
    nodeAgent:
      enable: true
      uploaderType: kopia
    velero:
      defaultPlugins:
        - openshift
        - aws
        - kubevirt
        - csi
      featureFlags:
        - EnableCSI

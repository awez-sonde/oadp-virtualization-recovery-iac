openshift-client*.tar.gz

# If you clone this repo again inside the checkout, Git creates a folder with the same name;
# never commit that nested tree into this repository.
/oadp-virtualization-recovery-iac/
